#!/bin/bash

### This script is called after the user uninstalls the application.

# 如果用户选择删除数据，清理应用数据目录
if [ "$wizard_data_action" = "delete" ] && [ -n "${TRIM_PKGVAR}" ]; then
    rm -rf "${TRIM_PKGVAR}" 2>/dev/null
fi

exit 0
