# MediaHub 代码优化清单

> 范围：按审查项 1/4/5/6/7/8/9/10/11/13/14 执行  
> 忽略：2（版本仓库元数据）、3（Webhook 公开）、12（CI/文档小问题）  
> 状态：`待处理` → `进行中` → `已完成` / `已验证`

| # | 问题 | 状态 | 涉及路径 | 验证方式 | 备注 |
|---|------|------|----------|----------|------|
| 1 | 启动插件失败被静默吞掉 | **已验证** | `server/plugins/scheduler.init.ts` | `npm run typecheck` | 分模块 try/catch + `log.info/error` |
| 4 | 中文注释/文档大面积乱码 | **已验证** | `accessible_paths.ts`、`definitions.ts`、`README.md`、关键新写模块 | 抽检 UTF-8 + typecheck | 关键路径已重写；历史大文件注释未全量扫清 |
| 5 | 前端页面过大 | **已验证** | `useStrmPathPicker.ts`、`organize_file.ts`、`settings.vue`、`organize.vue` | typecheck | 抽出 STRM 路径选择与整理文件工具；页面仍偏大，可继续拆卡片 |
| 6 | 后端重文件偏胖 | **已验证** | `server/lib/db/*` + `server/utils/db.ts` 门面；`server/utils/emby/proxy/*` + 反代插件 | typecheck | db 迁出 utils 子目录避免重复 auto-import |
| 7 | 前后端各一份 app_version | **已验证** | `config/app_version.ts`；两端 re-export | typecheck | 单一实现 |
| 8 | Nitro inline 过重 | **已验证** | `nuxt.config.ts` | typecheck | 重依赖改 external（含 telegram/wechat/sharp/grammy 等） |
| 9 | 依赖清理 | **已验证** | `package.json` | 依赖检索 + typecheck | 移除 `input`、`@nuxt/eslint`；`sharp` 改 dependencies |
| 10 | SQLite 同步 API 优化 | **已验证** | `server/lib/db/connection.ts` | typecheck | WAL/busy_timeout/索引；保持同步 API |
| 11 | 前端 console 过多 | **已验证** | `app/utils/client_log.ts` + 各页面组件 | typecheck | 生产收敛 debug/info |
| 13 | 空 catch 较多 | **已验证** | organize/logs/logger 等 | typecheck | 可观测或明确忽略注释 |
| 14 | 类型质量 | **已验证** | 启动插件、STRM composable、proxy error、accessible_paths 等 | typecheck | 触及文件改为 `unknown` / 明确接口 |

## 进度记录

### #1 启动插件
- [x] 实现
- [x] 验证（typecheck 通过）

### #7 app_version 合并
- [x] 实现
- [x] 验证

### #9 依赖清理
- [x] 实现
- [x] 验证（typecheck；建议本地再执行 `npm install` 刷新 lock）

### #8 Nitro inline
- [x] 实现
- [x] 验证

### #10 SQLite
- [x] 实现
- [x] 验证

### #13 空 catch
- [x] 实现
- [x] 验证

### #11 console
- [x] 实现
- [x] 验证

### #4 中文乱码
- [x] 实现（关键文件）
- [x] 验证

### #6 后端拆分
- [x] 实现
- [x] 验证

### #5 前端拆分
- [x] 实现（第一轮）
- [x] 验证

### #14 类型质量
- [x] 实现（触及范围）
- [x] 验证

## 最终验收
- [x] `npm run typecheck` 通过（exit 0）
- [x] 清单全部标记完成

## 主要变更结构

```
config/app_version.ts          # 版本唯一实现
server/lib/db/*                # DB 领域拆分
server/utils/db.ts             # 兼容门面
server/utils/emby/proxy/*      # 反代常量/类型/缓存
server/plugins/scheduler.init.ts
server/plugins/emby-proxy-server.ts
app/utils/client_log.ts
app/utils/organize_file.ts
app/composables/useStrmPathPicker.ts
docs/优化清单.md
```


## 中文编码复查（2026-07-24）

- 全库扫描 `app/` `server/` `docs/` `config/` `README.md`：130 个文本文件
- **未发现** 真实中文乱码片段（如历史误码形态）
- 源码中文注释与文案均为合法 **UTF-8**（fatal 解码通过）
- 此前“乱码”主要来自 Windows 终端默认代码页 **936(GBK)** 误读 UTF-8；用 UTF-8 打开正常
- 已补充：`.editorconfig`（charset=utf-8）、强化 `.gitattributes`（text eol=lf）
- 已规范化：去掉多余 BOM、源码换行统一 LF

## 后续可选（未纳入本次）
- 继续拆 `settings.vue` / `organize.vue` 为独立卡片组件
- 继续清理历史文件中文注释乱码
- 打包后对比 `.output/server` 体积（验证 external 策略）
- `npm install` 更新 `package-lock.json`
